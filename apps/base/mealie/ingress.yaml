apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mealie
spec:
  defaultBackend:
    service:
      name: mealie
      port:
        number: 9000
  ingressClassName: tailscale
  tls:
    - hosts:
        - mealie

