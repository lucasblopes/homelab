apiVersion: v1
kind: Pod
metadata:
  name: alpine-test-pod
  namespace: registry
spec:
  containers:
  - name: alpine
    # Change this to your DNS
    image: xxxx/my-alpine:latest
    command: ["echo", "Successfully pulled private image!"]
  imagePullSecrets:
  - name: registry-auth
  restartPolicy: "Never"
